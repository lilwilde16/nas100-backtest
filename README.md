# NAS100 Backtest

This repo auto-runs a simple NAS100 backtest.

## Manual run
Go to GitHub → Actions → NAS100 Backtest → Run workflow

## Scheduled run
The workflow runs daily by schedule.

## Results
Download the `nas100-backtest-results` artifact from the workflow run.
